# 个人代码资产库 v0.1 隐私说明

个人代码资产库采用 Local-first 设计。v0.1 的核心功能在本机完成，不需要账号或网络。

## 本机处理

当前 App：

- 不上传代码或代码库；
- 不上传搜索词、标签、Project 或使用记录；
- 不包含 Telemetry 或 Analytics；
- 不进行远程 Crash Upload；
- 不调用 AI；
- 不使用 Cloud 服务或同步；
- 不执行保存或导入的代码；
- 不要求网络连接。

搜索索引、Secret 提示和 Exact Duplicate 检查都在本机完成。普通代码库文件是正式数据，SQLite 只用于本地派生搜索索引。

## App 可以访问哪些文件

App 只围绕你通过 macOS 系统选择器明确选择的位置工作：

- 当前代码库文件夹；
- 你主动选择的单个导入文件；
- 你选择的备份保存位置；
- 你选择的恢复备份。

App 不自动扫描整个 Home 目录，也不自动扫描项目文件夹。

## 单文件导入

导入时，App 对所选文件进行只读检查和读取：

- 不修改、删除或重命名源文件；
- 不执行源文件中的代码；
- 不上传文件或文件内容；
- 不要求源文件在导入后继续存在。

保存成功后，代码副本进入你自己的代码库。具体来源只保存必要的来源说明或文件名，不把原文件绝对路径作为继续使用资产的条件。

## Sensitive Guardrail

App 会阻止明显的凭据文件名，例如 `.env`、credentials、`.pem` 和 `.key`。对于普通代码中看起来像 Secret 的片段，App 会在保存前给出脱敏警告。

这些检查是降低误存风险的辅助措施，不是完整的安全审计工具，也不能保证代码中没有任何 Secret。保存、分享或备份代码前，仍应由你检查内容并撤销已经暴露的真实凭据。

## 备份与恢复

App 创建的本地备份包含代码库中的正式数据，可能包含完整代码、笔记、来源、验证记录、废纸篓和 Project 关系。

**App 不会对备份额外加密。** 如果代码或笔记中含有敏感信息，请把备份保存在受信任的位置，并使用你自己的磁盘加密、访问控制和离线保管方式。v0.1 不提供密码保护、端到端加密或云端备份。

恢复前，App 会验证备份，并为当前代码库创建恢复副本。恢复副本同样可能包含敏感代码，也应按备份文件保护。

## 日志和系统数据

产品日志边界不允许默认记录完整代码、Secret、密码或 Token。macOS 和 Tauri 可能产生正常的本机偏好或缓存，但它们不是代码库的 Source of Truth，删除 App 不应删除用户选择的代码库。

## 权限

v0.1 不申请摄像头、麦克风、位置、联系人、日历或屏幕录制权限。文件访问由你在系统选择器中的明确操作限定。

## 发布范围

v0.1 面向 Apple Silicon Mac 和 macOS 11.0 及以上版本，采用个人本地安装方式。目前不提供公开下载、Mac App Store 发布、自动更新或 Intel Mac 兼容保证。
