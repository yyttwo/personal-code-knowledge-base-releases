# 个人代码资产库 v0.3.0 用户指南

本指南面向第一次使用本 App 的用户。核心资产管理和普通搜索不需要账号或联网；Ollama、DeepSeek 与 Qwen AI 功能均为可选能力。

## 一、App 是什么

个人代码资产库用于保存值得以后查找、理解和复用的代码。每条“代码资产”可以同时记录代码、用途、来源、标签、验证情况和使用过的 Project。

数据保存在你自己选择的代码库文件夹中。App 本身和代码库是两件不同的东西：App 可以重装，代码库仍留在原位置。

## 二、第一次启动

启动后会看到两个入口：

### 创建新的代码库

1. 点击“创建新的代码库”。
2. 在 macOS 文件夹选择器中选择一个空文件夹。
3. App 会在该文件夹中建立代码库所需的普通文件。

推荐位置是 `~/Documents/CodeLibrary`，也可以选择你有权限访问、容易找到并能自行备份的其他本地位置。不要选择装有其他文件的文件夹，也不要把代码库放进 App Bundle 内。

### 打开已有代码库

1. 点击“打开已有代码库”。
2. 选择代码库最外层文件夹，也就是包含 `library.json` 和 `entries` 的文件夹。
3. 打开后，资产、Project 和搜索功能会从该代码库加载。

App 不会自动扫描 Home 目录，也不会替你寻找代码库。

## 三、创建代码资产

点击右上角“＋ 新建资产”。创建时只有六项业务必填：

1. **代码**：要保存的纯文本代码。
2. **标题**：以后能快速认出的名称。
3. **语言**：例如 `python`、`typescript`、`shell` 或 `sql`。
4. **类型**：例如代码片段、函数、命令、配置、查询、模板、示例、问题修复或模式。
5. **一句话说明**：说明这段代码做什么。
6. **分类**：你自己的大类，例如“文件处理”或“数据库”。

其他信息按需填写：

- **标签**：用逗号分隔，适合以后精确组合筛选。
- **具体来源**：记录文件名、网页说明或其他线索；不会与原文件建立同步关系。
- **关联项目**：一条资产可以关联一个、多个或不关联 Project。
- **补充知识**：工作原理、适用与不适用场景、输入输出、依赖、兼容性和常见问题。
- **来源与许可**：来源类型、URL、作者和许可。
- **知识状态**：新资产默认是“待学习”。保存后编辑资产，可改为“已理解”“已验证”“已复用”或“稳定资产”。状态不会自动升级。
- **验证记录**：新资产默认显示“未验证”。只有你在编辑时明确点击记录，本 App 才保存验证时间。

点击保存后，App 会先在本机进行保存前检查。潜在 Secret 或完全重复的代码会显示说明；普通格式错误仍需先修正。App 不会运行你保存的代码。

## 四、导入代码文件

当前版本一次导入一个文件。

1. 点击“导入代码文件”。
2. 在系统选择器中选择一个纯文本代码文件，例如 `.py`。
3. App 会预填标题、语言、代码和具体来源中的文件名。
4. 检查内容，并补充一句话说明、类型和分类。
5. 需要时选择 Project，然后按普通保存流程完成。

导入只读取你选择的源文件。App 不修改、不删除、不重命名、不执行，也不上传源文件；保存后，代码副本位于你的代码库中，不依赖原文件继续存在。

明显敏感的文件名，例如 `.env`、credentials、`.pem` 或 `.key`，会在读取前被阻止。`.rtf` 和 `.rtfd` 是富文本文档，不是纯文本代码文件，不能导入。需要先在原编辑器中另存为纯文本代码文件。

## 五、搜索

顶部搜索框可搜索标题、一句话说明、代码和其他已索引文字。使用多关键词搜索时，以空格分隔关键词；结果需要同时匹配这些关键词。

还可以使用语言、分类、类型和知识状态筛选。标签框是**精确标签筛选**：输入多个逗号分隔标签时，资产需要同时包含这些标签。

打开一个 Project 后，在“项目资产”区域使用“在此项目中全文搜索”。这只搜索该 Project 已关联的资产；旁边的标签框仍是精确标签筛选。

如果已保存的资产找不到，请到“设置”点击“重建搜索索引”。重建只处理派生索引，不会删除代码。

## 六、配置 AI

AI 默认关闭。进入“设置”后，可以分别配置 Generation Provider 与 Embedding Provider。

### Generation Provider

Generation 用于 AI 内容辅助和 AI 对话。可选择：

- **Ollama：** 使用本机模型，需要先自行安装、启动 Ollama 并下载模型。
- **DeepSeek：** 使用 DeepSeek API，需要填写自己的 API Key。
- **Qwen：** 使用千问 AI 平台 API，需要填写自己的 API Key。

选择 Provider 后，确认 Endpoint 和生成模型，填写 API Key（云端 Provider），点击“保存 AI 设置”，再点击“测试连接”。AI 内容辅助和 AI 对话会自动使用这里保存的 Provider 与模型，AI 对话页面不需要再次选择 Provider。

API Key 只保存在 macOS 钥匙串，不会写入代码库或普通配置文件。首次读取钥匙串时，macOS 可能要求输入登录密码并点击“允许”或“始终允许”。

### Embedding Provider

Embedding 只用于语义索引、智能搜索和相关代码推荐，可独立选择 Ollama 或 Qwen API。它不会随 Generation Provider 自动改变。

Qwen Embedding 默认使用 `text-embedding-v4`，维度为 1024。先测试 Embedding 连接；成功后再构建候选索引、完成校验并激活。更换 Provider 或模型后，需要重新构建语义索引。

### 云端数据提示

使用 DeepSeek 或 Qwen 时，完成当前 AI 请求所需的代码或文字会发送给所选服务商。使用 Qwen API 构建语义索引时，待索引资产的相关文本也会分段发送到 Qwen Embedding 服务。请勿发送真实凭据、私钥或不适合交给第三方的代码。

## 七、AI 内容辅助与 AI 对话

在资产页面打开“AI 辅助”后，可以请求模型解释代码或生成建议。生成结果只会先显示预览，不会自动覆盖资产内容；请核对后再决定是否保存。

侧边栏“AI 对话”用于直接与当前 Generation Provider 对话学习。若页面提示模型未选择，请返回“设置”，确认已经保存生成模型，而不只是通过连接测试。

AI 输出可能不准确。涉及生产操作、安全配置、数据删除或凭据处理时，应由用户自行验证。

## 八、Project

### 创建与关联

- 在侧栏点击“新建项目”，填写项目名称和可选说明。
- 新建或编辑资产时，在“关联项目”中选择一个或多个 Project。
- 同一资产可以同时用于多个 Project，资产本身只保存一份。

### 改名、状态与归档

打开 Project 后可以：

- 编辑名称与说明；改名不会改变关联关系。
- 把状态设为“进行中”“已暂停”或“已完成”。
- 归档暂时不需要显示的 Project。归档不会删除 Project、资产或关联，可以恢复到项目列表。

### 完成复盘

点击“开始项目代码资产复盘”可查看待继续学习、已验证、已复用、常用、缺少说明、缺少标签和跨 Project 复用等线索。复盘是只读的，不会自动改变资产状态、验证记录或常用标记。

## 九、废纸篓

删除资产时，App 会先把它移到废纸篓，之后仍可恢复。

1. 在资产详情中选择移到废纸篓并确认。
2. 在侧栏或“设置”打开废纸篓。
3. 点击“恢复”让资产回到代码库。

“永久删除”无法通过 App 撤销，必须再次明确确认。确认前请确保已经有需要的备份。

## 十、创建备份

1. 打开“设置”。
2. 在“本地备份”查看备份范围和未加密提示。
3. 点击“创建备份”。
4. 在保存面板中选择代码库外的安全位置和一个尚不存在的名称。
5. 等待“备份完成”，记下或复制完整保存路径。

备份包含资产、说明、分类、标签、来源、验证记录、笔记、示例、废纸篓、Project 和关联关系。搜索索引不需要进入备份，因为可以重建。

App 不会给备份额外加密。备份里可能包含完整代码，请放在只有你能访问并有适当磁盘保护的位置。

## 十一、从备份恢复

恢复会让**当前打开的代码库**回到备份创建时的状态。备份之后新增或修改的内容不会出现在恢复后的当前代码库中。

1. 先确认当前打开的是要恢复的代码库；如果需要保留原件，可先创建一个新的独立代码库作为恢复目标。
2. 打开“设置”，点击“选择并校验备份”。
3. 选择完整的备份目录。App 会先检查格式、兼容性和完整性。
4. 阅读确认窗口。开始恢复前，App 会创建当前代码库的“恢复副本”。
5. 点击“创建恢复前备份并恢复”。
6. 看到“代码库恢复成功”后检查资产和 Project。
7. 如果另有“搜索索引需要重建”的提示，数据恢复已经成功；点击“重建搜索索引”即可恢复搜索。

不要手工修改备份中的文件，也不要用不完整的备份目录恢复。

## 十二、移动代码库

1. 退出 App，避免复制期间仍有写入。
2. 在 Finder 中复制或移动**整个代码库文件夹**到新位置。
3. 重新启动 App，选择“打开已有代码库”。
4. 选择新位置的代码库根目录。
5. 检查资产和 Project；搜索异常时重建索引。

代码库不依赖旧位置。导入资产保存的是代码副本和来源说明，不要求原导入文件继续存在。

## 十三、重建搜索索引

当搜索结果缺失、索引损坏或移动代码库后搜索异常时：

1. 打开代码库。
2. 进入“设置”。
3. 点击“重建搜索索引”。
4. 等待成功提示，再重新搜索。

`index.sqlite` 只是搜索索引。重建不会改变资产、Project、废纸篓或备份。

## 十四、卸载或重装后重新打开代码库

删除 `/Applications/个人代码资产库.app` 只删除 App，不会删除你另行选择的代码库文件夹。

重新安装后：

1. 从“应用程序”启动个人代码资产库。
2. 点击“打开已有代码库”。
3. 选择原来的代码库根目录。
4. 如搜索异常，在“设置”重建搜索索引。

请自己记住代码库位置，并把代码库与重要备份纳入日常文件保护习惯。
